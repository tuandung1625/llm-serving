services:
  model:
    image: vllm/vllm-openai:nightly
    entrypoint: [python3, -m, vllm.entrypoints.openai.api_server]
    command:
      - --model=/model
      - --served-model-name=LFM2.5-1.2B-Instruct
      - --host=0.0.0.0
      - --port=8000
      - --dtype=bfloat16
      - --quantization=fp8
      - --kv-cache-dtype=fp8
      - --tensor-parallel-size=1
      - --gpu-memory-utilization=0.92
      # Max model length sweep. Pick the smallest value above max trace length + chat template overhead.
      - --max-model-len=12288
      # - --max-model-len=4096
      # - --max-model-len=8192
      # - --max-model-len=10240
      # - --max-model-len=16384
      # - --max-model-len=24576
      # - --max-model-len=32768
      - --max-num-seqs=32
      - --max-num-batched-tokens=12288
      - --enable-prefix-caching
      - --mamba-cache-mode=align
      - --mamba-cache-dtype=auto
      - --enable-chunked-prefill
      - --async-scheduling
      - --performance-mode=interactivity
      - --prefix-caching-hash-algo=sha256
      - --no-enable-log-requests
      - --disable-log-stats
    environment: {OMP_NUM_THREADS: "1", MKL_NUM_THREADS: "1", TOKENIZERS_PARALLELISM: "false", PYTHONUNBUFFERED: "1"}
    ports: ["8000:8000"]
    shm_size: "2g"
    deploy:
      resources:
        reservations:
          devices:
            - {driver: nvidia, count: 1, capabilities: [gpu]}

